spring:
  redis:
    host: localhost
    port: 6379
    expire:
      authCode: 60 # 验证码超期时间
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/yujia?characterEncoding=utf-8
    username: root
    password:
server:
  port: 8888

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
